// import {sequencer} from '.';

describe('sequencer', () => {
  // const numbers = [0, 1, 2];
  // const objects = [{a:1}, {a:1}, {a:1}];

  it('wraps around its steps', () => {
    expect('todo').toBe(false);
  });
  it('sets its internal cursor to given existing step', () => {
    expect('todo').toBe(false);
  });
  it('yields next value when given invalid step', () => {
    expect('todo').toBe(false);
  });

});